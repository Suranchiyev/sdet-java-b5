package day40;

public class Student {
	// Access modifiers used to control access of class level variables, methods, classes.
	// public    - accessible from everywhere.
	// protected - accessible within the same package and inside the subclass.
	// default   - accessible within the same package only.
	// private   - accessible within the same class only.
	
	public String name;
	protected String course; 
	double grade;
	private int age;
}

